-- Manual bootstrap for the Sahayak database (optional; init_db() does this too).
CREATE EXTENSION IF NOT EXISTS vector;

-- Tables are created by SQLAlchemy (database.connection.init_db). This file is
-- provided for DBAs who prefer to provision the extension out of band.
